diff --git a/chrome/browser/ui/toolbar/chrome_labs/chrome_labs_utils.cc b/chrome/browser/ui/toolbar/chrome_labs/chrome_labs_utils.cc
index 3c310d91b5591..0fc554a0ee3ec 100644
--- a/chrome/browser/ui/toolbar/chrome_labs/chrome_labs_utils.cc
+++ b/chrome/browser/ui/toolbar/chrome_labs/chrome_labs_utils.cc
@@ -1,4 +1,4 @@
-// Copyright 2021 The Chromium Authors
+// Copyright 2026 The Chromium Authors and 同
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
@@ -26,7 +26,7 @@
 #endif
 
 namespace {
-bool force_activation_for_testing = false;
+bool force_activation_for_testing = true;
 }
 
 bool IsFeatureSupportedOnChannel(const LabInfo& lab) {
@@ -139,8 +139,14 @@ bool AreNewChromeLabsExperimentsAvailable(Profile* profile) {
 }
 
 bool IsChromeLabsEnabled() {
+  if (base::CommandLine::ForCurrentProcess()->HasSwitch(
+          "disable-chrome-labs")) {
+    return false;
+  }
+
   // Always early out on the stable channel regardless of other conditions.
-  if (chrome::GetChannel() == version_info::Channel::STABLE) {
+  if (chrome::GetChannel() == version_info::Channel::STABLE &&
+      !force_activation_for_testing) {
     return false;
   }
   if (base::CommandLine::ForCurrentProcess()->HasSwitch(
@@ -160,7 +166,7 @@ bool IsChromeLabsEnabled() {
   }
 
   // The percentage of users that should see the feature.
-  const int kChromeLabsActivationPercentage = 99;
+  constexpr int kChromeLabsActivationPercentage = 100;
 
   if (force_activation_for_testing ||
       g_browser_process->local_state()->GetInteger(
