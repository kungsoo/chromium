diff --git a/chrome/browser/ui/tabs/features.cc b/chrome/browser/ui/tabs/features.cc
index df3758a6a88c4..85c3815e6dc5a 100644
--- a/chrome/browser/ui/tabs/features.cc
+++ b/chrome/browser/ui/tabs/features.cc
@@ -93,7 +93,10 @@ BASE_FEATURE_PARAM(base::TimeDelta,
 
 BASE_FEATURE(kTabSelectionByPointer, base::FEATURE_ENABLED_BY_DEFAULT);
 
-BASE_FEATURE(kHorizontalTabStripComboButton, base::FEATURE_ENABLED_BY_DEFAULT);
+BASE_FEATURE(kThoriumLeftAlignedTabSearchButton,
+             base::FEATURE_DISABLED_BY_DEFAULT);
+
+BASE_FEATURE(kHorizontalTabStripComboButton, base::FEATURE_DISABLED_BY_DEFAULT);
 BASE_FEATURE_PARAM(bool,
                    kHorizontalTabStripComboButtonShowStartOnly,
                    &kHorizontalTabStripComboButton,
diff --git a/chrome/browser/ui/tabs/features.h b/chrome/browser/ui/tabs/features.h
index 2603e02ca8967..6c790293ed277 100644
--- a/chrome/browser/ui/tabs/features.h
+++ b/chrome/browser/ui/tabs/features.h
@@ -89,6 +89,8 @@ BASE_DECLARE_FEATURE_PARAM(
 
 BASE_DECLARE_FEATURE(kTabSelectionByPointer);
 
+BASE_DECLARE_FEATURE(kThoriumLeftAlignedTabSearchButton);
+
 BASE_DECLARE_FEATURE(kBackToOpener);
 
 BASE_DECLARE_FEATURE(kHorizontalTabStripComboButton);
diff --git a/chrome/browser/ui/tabs/tab_strip_prefs.cc b/chrome/browser/ui/tabs/tab_strip_prefs.cc
index b0d357c347745..dc367d3e40bf6 100644
--- a/chrome/browser/ui/tabs/tab_strip_prefs.cc
+++ b/chrome/browser/ui/tabs/tab_strip_prefs.cc
@@ -28,12 +28,9 @@ std::optional<bool> g_tab_search_trailing_tabstrip_at_startup;
 namespace tabs {
 
 bool GetDefaultTabSearchRightAligned() {
-  // These platforms are all left aligned, the others should be right.
-#if BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_WIN)
-  return false;
-#else
-  return true;
-#endif
+  // Thorium keeps tab search on the physical right by default on every
+  // desktop platform, with an explicit feature available for left alignment.
+  return !base::FeatureList::IsEnabled(kThoriumLeftAlignedTabSearchButton);
 }
 
 void RegisterProfilePrefs(user_prefs::PrefRegistrySyncable* registry) {
