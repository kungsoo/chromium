diff --git a/chrome/browser/ui/ui_features.cc b/chrome/browser/ui/ui_features.cc
index 683eae9059294..c4b39b3526571 100644
--- a/chrome/browser/ui/ui_features.cc
+++ b/chrome/browser/ui/ui_features.cc
@@ -146,6 +146,11 @@ BASE_FEATURE(kPreloadTopChromeWebUILessNavigations,
 BASE_FEATURE(kPressAndHoldEscToExitBrowserFullscreen,
              base::FEATURE_ENABLED_BY_DEFAULT);
 
+// Enables closing tabs with a right click. Shift+right-click keeps the tab
+// context menu available.
+BASE_FEATURE(kRightClickToCloseTab,
+             base::FEATURE_DISABLED_BY_DEFAULT);
+
 #if BUILDFLAG(IS_WIN)
 // Enables the UI for Process Isolation in chrome://settings/system.
 BASE_FEATURE(kProcessIsolationSettings, base::FEATURE_DISABLED_BY_DEFAULT);
diff --git a/chrome/browser/ui/ui_features.h b/chrome/browser/ui/ui_features.h
index 1466c54bc0364..282691e5cfb63 100644
--- a/chrome/browser/ui/ui_features.h
+++ b/chrome/browser/ui/ui_features.h
@@ -376,6 +376,9 @@ BASE_DECLARE_FEATURE_PARAM(bool, kTabGroupsFocusingPinnedTabs);
 BASE_DECLARE_FEATURE_PARAM(bool, kTabGroupsFocusingAutoClose);
 BASE_DECLARE_FEATURE_PARAM(bool, kTabGroupsFocusingDefaultToFocused);
 
+// Enables closing tabs with a right click. Shift+right-click opens the menu.
+BASE_DECLARE_FEATURE(kRightClickToCloseTab);
+
 BASE_DECLARE_FEATURE(kVerticalTabsGrabHandleRemoval);
 BASE_DECLARE_FEATURE_PARAM(bool, kVerticalTabsGrabHandleRemovalAlways);
 
diff --git a/chrome/browser/ui/views/tabs/tab.cc b/chrome/browser/ui/views/tabs/tab.cc
index cba272c4b497e..4b69f347fc260 100644
--- a/chrome/browser/ui/views/tabs/tab.cc
+++ b/chrome/browser/ui/views/tabs/tab.cc
@@ -23,6 +23,7 @@
 #include "base/numerics/safe_conversions.h"
 #include "base/scoped_observation.h"
 #include "base/strings/utf_string_conversions.h"
+#include "base/task/sequenced_task_runner.h"
 #include "base/time/time.h"
 #include "build/build_config.h"
 #include "cc/paint/paint_flags.h"
@@ -611,10 +612,21 @@ bool IsSelectionModifierDown(const ui::MouseEvent& event) {
 
 bool Tab::OnMousePressed(const ui::MouseEvent& event) {
   shift_pressed_on_mouse_down_ = event.IsShiftDown();
+  suppress_context_menu_ = false;
   controller_->UpdateHoverCard(nullptr,
                                TabSlotController::HoverCardUpdateType::kEvent);
   controller_->OnMouseEventInTab(this, event);
 
+  // Right-click closes the tab when enabled. Shift+right-click keeps the
+  // normal tab context menu, and touch-generated right-clicks keep drag support.
+  if (event.IsOnlyRightMouseButton() &&
+      base::FeatureList::IsEnabled(features::kRightClickToCloseTab) &&
+      !event.IsShiftDown() && !(event.flags() & ui::EF_FROM_TOUCH)) {
+    // Suppress the context menu on release; close only after confirming the
+    // pointer is still over this tab.
+    suppress_context_menu_ = true;
+  }
+
   // Allow a right click from touch to drag, which corresponds to a long click.
   if (event.IsOnlyLeftMouseButton() ||
       (event.IsOnlyRightMouseButton() && event.flags() & ui::EF_FROM_TOUCH)) {
@@ -666,10 +678,39 @@ void Tab::OnMouseReleased(const ui::MouseEvent& event) {
   // so, bail immediately, since our members are already dead and we shouldn't
   // do anything else except drop the tab where it is.
   if (controller_->EndDrag(EndDragReason::kComplete)) {
+    shift_pressed_on_mouse_down_ = false;
+    suppress_context_menu_ = false;
+    return;
+  }
+
+  // Close on right-click release if the press opted out of the context menu.
+  // Posting avoids reentrancy hazards with platform menu/mouse handling.
+  if (event.IsOnlyRightMouseButton() &&
+      base::FeatureList::IsEnabled(features::kRightClickToCloseTab) &&
+      suppress_context_menu_ && HitTestPoint(event.location()) && !closing()) {
+    base::SequencedTaskRunner::GetCurrentDefault()->PostTask(
+        FROM_HERE,
+        base::BindOnce(
+            [](base::WeakPtr<Tab> tab) {
+              if (tab && !tab->closing()) {
+                tab->controller()->CloseTab(tab.get(),
+                                           CloseTabSource::kFromMouse);
+              }
+            },
+            weak_ptr_factory_.GetWeakPtr()));
+
+    // Keep suppress_context_menu_ set so the context menu controller ignores
+    // the matching release event.
     shift_pressed_on_mouse_down_ = false;
     return;
   }
 
+  if (event.IsOnlyRightMouseButton()) {
+    shift_pressed_on_mouse_down_ = false;
+    suppress_context_menu_ = false;
+    return;
+  }
+
   // Close tab on middle click, but only if the button is released over the tab
   // (normal windows behavior is to discard presses of a UI element where the
   // releases happen off the element).
@@ -710,6 +751,9 @@ void Tab::OnMouseReleased(const ui::MouseEvent& event) {
 
 void Tab::OnMouseCaptureLost() {
   controller_->EndDrag(EndDragReason::kCaptureLost);
+  // Reset mouse state when capture is lost to prevent stale menu suppression.
+  shift_pressed_on_mouse_down_ = false;
+  suppress_context_menu_ = false;
 }
 
 void Tab::OnMouseMoved(const ui::MouseEvent& event) {
diff --git a/chrome/browser/ui/views/tabs/tab.h b/chrome/browser/ui/views/tabs/tab.h
index 4bb45b158f396..302c0c71785bc 100644
--- a/chrome/browser/ui/views/tabs/tab.h
+++ b/chrome/browser/ui/views/tabs/tab.h
@@ -180,6 +180,9 @@ class Tab : public gfx::AnimationDelegate,
 
   bool mouse_hovered() const { return mouse_hovered_; }
 
+  // Returns true when the context menu should be skipped for this gesture.
+  bool ShouldSuppressContextMenu() const { return suppress_context_menu_; }
+
   void ShowHover(TabStyle::ShowHoverStyle style);
   void HideHover(TabStyle::HideHoverStyle style);
 
@@ -337,6 +340,10 @@ class Tab : public gfx::AnimationDelegate,
   // up.
   bool shift_pressed_on_mouse_down_ = false;
 
+  // Set between a right-button press and release when right-click-to-close is
+  // enabled and the gesture should not open the tab context menu.
+  bool suppress_context_menu_ = false;
+
   std::unique_ptr<TabCloseButtonObserver> tab_close_button_observer_;
 
   std::unique_ptr<tabs::TabDataObserver> tab_data_observer_;
diff --git a/chrome/browser/ui/views/tabs/tab_strip.cc b/chrome/browser/ui/views/tabs/tab_strip.cc
index dc2fe4f2e1f7e..a1a4cea1b573a 100644
--- a/chrome/browser/ui/views/tabs/tab_strip.cc
+++ b/chrome/browser/ui/views/tabs/tab_strip.cc
@@ -2203,6 +2203,9 @@ void TabStrip::TabContextMenuController::ShowContextMenuForViewImpl(
   if (tab->closing()) {
     return;
   }
+  if (tab->ShouldSuppressContextMenu()) {
+    return;
+  }
   parent_->ShowContextMenuForTab(tab, point, source_type);
 }
 
diff --git a/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.cc b/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.cc
index 468c9d61b4ecb..0640815463641 100644
--- a/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.cc
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.cc
@@ -8,12 +8,14 @@
 #include <string>
 
 #include "base/command_line.h"
+#include "base/feature_list.h"
 #include "base/functional/callback_helpers.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/user_metrics.h"
 #include "base/metrics/user_metrics_action.h"
 #include "base/notimplemented.h"
 #include "base/strings/utf_string_conversions.h"
+#include "base/task/sequenced_task_runner.h"
 #include "base/time/time.h"
 #include "chrome/browser/glic/browser_ui/tab_underline_controller.h"
 #include "chrome/browser/glic/browser_ui/tab_underline_view.h"
@@ -30,6 +32,7 @@
 #include "chrome/browser/ui/tabs/tab_muted_utils.h"
 #include "chrome/browser/ui/tabs/tab_strip_model.h"
 #include "chrome/browser/ui/tabs/tab_style.h"
+#include "chrome/browser/ui/ui_features.h"
 #include "chrome/browser/ui/tabs/vertical_tab_strip_state_controller.h"
 #include "chrome/browser/ui/views/event_utils.h"
 #include "chrome/browser/ui/views/frame/browser_frame_view.h"
@@ -361,7 +364,15 @@ bool VerticalTabView::OnMousePressed(const ui::MouseEvent& event) {
 
   auto* controller = collection_node_->GetController();
   shift_pressed_on_mouse_down_ = event.IsShiftDown();
+  suppress_context_menu_ = false;
   RecordMousePressedInTab();
+  // Right-click closes the tab when enabled. Shift+right-click keeps the
+  // normal tab context menu, and touch-generated right-clicks keep drag support.
+  if (event.IsOnlyRightMouseButton() &&
+      base::FeatureList::IsEnabled(features::kRightClickToCloseTab) &&
+      !event.IsShiftDown() && !(event.flags() & ui::EF_FROM_TOUCH)) {
+    suppress_context_menu_ = true;
+  }
   UpdateHoverCard(nullptr, TabSlotController::HoverCardUpdateType::kEvent);
 
   // Capture the selection model before selection changes.
@@ -397,6 +408,34 @@ void VerticalTabView::OnMouseReleased(const ui::MouseEvent& event) {
 
   auto* controller = collection_node_->GetController();
   base::WeakPtr<VerticalTabView> self = weak_ptr_factory_.GetWeakPtr();
+  if (event.IsOnlyRightMouseButton() &&
+      base::FeatureList::IsEnabled(features::kRightClickToCloseTab) &&
+      suppress_context_menu_ && HitTestPoint(event.location())) {
+    base::SequencedTaskRunner::GetCurrentDefault()->PostTask(
+        FROM_HERE,
+        base::BindOnce(
+            [](base::WeakPtr<VerticalTabView> tab) {
+              if (tab && tab->collection_node_) {
+                if (auto* controller = tab->collection_node_->GetController()) {
+                  controller->CloseTab(tab->GetTabInterface());
+                }
+              }
+            },
+            weak_ptr_factory_.GetWeakPtr()));
+
+    // Keep suppress_context_menu_ set so the context menu hook ignores the
+    // matching release event.
+    shift_pressed_on_mouse_down_ = false;
+    controller->GetDragHandler().EndDrag(EndDragReason::kCancel);
+    return;
+  }
+
+  if (event.IsOnlyRightMouseButton()) {
+    shift_pressed_on_mouse_down_ = false;
+    suppress_context_menu_ = false;
+    controller->GetDragHandler().EndDrag(EndDragReason::kCancel);
+    return;
+  }
   if (event.IsOnlyMiddleMouseButton() ||
       // Close tab on double click, mirror of IsOnlyMiddleMouseButton.
       // Based on gz83's work.
@@ -419,6 +458,7 @@ void VerticalTabView::OnMouseReleased(const ui::MouseEvent& event) {
     return;
   }
   shift_pressed_on_mouse_down_ = false;
+  suppress_context_menu_ = false;
 }
 
 void VerticalTabView::OnMouseMoved(const ui::MouseEvent& event) {
@@ -894,6 +934,9 @@ void VerticalTabView::ShowContextMenuForViewImpl(
     views::View* source,
     const gfx::Point& point,
     ui::mojom::MenuSourceType source_type) {
+  if (suppress_context_menu_) {
+    return;
+  }
   if (collection_node_) {
     if (auto* controller = collection_node_->GetController()) {
       controller->ShowContextMenuForNode(collection_node_, source, point,
diff --git a/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.h b/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.h
index 30bddac23a66d..7f7e9e12c8d5f 100644
--- a/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.h
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.h
@@ -242,6 +242,9 @@ class VerticalTabView : public views::View,
   bool pinned_ = false;
   bool shift_pressed_on_mouse_down_ = false;
 
+  // Set between a right-button press and release when right-click-to-close is
+  // enabled and the gesture should not open the tab context menu.
+  bool suppress_context_menu_ = false;
   std::unique_ptr<GlowHoverController> hover_controller_;
   float hover_opacity_min_;
   float hover_opacity_max_;
