diff --git a/chrome/browser/ui/ui_features.cc b/chrome/browser/ui/ui_features.cc
index c4b39b3526571..ae57160ecd79e 100644
--- a/chrome/browser/ui/ui_features.cc
+++ b/chrome/browser/ui/ui_features.cc
@@ -151,6 +151,16 @@ BASE_FEATURE(kPressAndHoldEscToExitBrowserFullscreen,
 BASE_FEATURE(kRightClickToCloseTab,
              base::FEATURE_DISABLED_BY_DEFAULT);
 
+// Enables activating tabs by hovering the mouse over them for a configured
+// duration.
+BASE_FEATURE(kHoverActivateTab,
+             base::FEATURE_DISABLED_BY_DEFAULT);
+BASE_FEATURE_PARAM(int,
+                   kHoverActivateTabDelayMs,
+                   &kHoverActivateTab,
+                   "hover_delay_ms",
+                   500);
+
 #if BUILDFLAG(IS_WIN)
 // Enables the UI for Process Isolation in chrome://settings/system.
 BASE_FEATURE(kProcessIsolationSettings, base::FEATURE_DISABLED_BY_DEFAULT);
diff --git a/chrome/browser/ui/ui_features.h b/chrome/browser/ui/ui_features.h
index 282691e5cfb63..d2b5d9e87fa51 100644
--- a/chrome/browser/ui/ui_features.h
+++ b/chrome/browser/ui/ui_features.h
@@ -379,6 +379,9 @@ BASE_DECLARE_FEATURE_PARAM(bool, kTabGroupsFocusingDefaultToFocused);
 // Enables closing tabs with a right click. Shift+right-click opens the menu.
 BASE_DECLARE_FEATURE(kRightClickToCloseTab);
 
+BASE_DECLARE_FEATURE(kHoverActivateTab);
+BASE_DECLARE_FEATURE_PARAM(int, kHoverActivateTabDelayMs);
+
 BASE_DECLARE_FEATURE(kVerticalTabsGrabHandleRemoval);
 BASE_DECLARE_FEATURE_PARAM(bool, kVerticalTabsGrabHandleRemovalAlways);
 
diff --git a/chrome/browser/ui/views/tabs/tab.cc b/chrome/browser/ui/views/tabs/tab.cc
index 4b69f347fc260..e56f0ea663e4a 100644
--- a/chrome/browser/ui/views/tabs/tab.cc
+++ b/chrome/browser/ui/views/tabs/tab.cc
@@ -773,9 +773,19 @@ void Tab::OnMouseMoved(const ui::MouseEvent& event) {
 
 void Tab::OnMouseEntered(const ui::MouseEvent& event) {
   MaybeUpdateHoverStatus(event);
+
+  if (base::FeatureList::IsEnabled(features::kHoverActivateTab) &&
+      !IsActive()) {
+    hover_activate_timer_.Start(
+        FROM_HERE, base::Milliseconds(features::kHoverActivateTabDelayMs.Get()),
+        base::BindOnce(&Tab::OnHoverActivateTimeout,
+                       weak_ptr_factory_.GetWeakPtr()));
+  }
 }
 
 void Tab::OnMouseExited(const ui::MouseEvent& event) {
+  hover_activate_timer_.Stop();
+
   if (!mouse_hovered_) {
     return;
   }
@@ -783,6 +793,14 @@ void Tab::OnMouseExited(const ui::MouseEvent& event) {
   controller_->HideHover(this, TabStyle::HideHoverStyle::kGradual);
 }
 
+void Tab::OnHoverActivateTimeout() {
+  if (!IsActive() && mouse_hovered_) {
+    ui::MouseEvent synthetic_event(ui::EventType::kMouseMoved, gfx::Point(),
+                                   gfx::Point(), base::TimeTicks::Now(), 0, 0);
+    controller_->SelectTab(this, synthetic_event);
+  }
+}
+
 void Tab::OnGestureEvent(ui::GestureEvent* event) {
   controller_->UpdateHoverCard(nullptr,
                                TabSlotController::HoverCardUpdateType::kEvent);
diff --git a/chrome/browser/ui/views/tabs/tab.h b/chrome/browser/ui/views/tabs/tab.h
index 302c0c71785bc..4eee7407d9cad 100644
--- a/chrome/browser/ui/views/tabs/tab.h
+++ b/chrome/browser/ui/views/tabs/tab.h
@@ -11,6 +11,7 @@
 
 #include "base/gtest_prod_util.h"
 #include "base/memory/raw_ptr.h"
+#include "base/timer/timer.h"
 #include "chrome/browser/ui/tabs/tab_data.h"
 #include "chrome/browser/ui/tabs/tab_enums.h"
 #include "chrome/browser/ui/views/tabs/hovercard/hover_card_anchor_target.h"
@@ -269,6 +270,9 @@ class Tab : public gfx::AnimationDelegate,
   // events are disabled because of touch input, this is a no-op.
   void MaybeUpdateHoverStatus(const ui::MouseEvent& event);
 
+  // Called when the hover-to-activate timer expires.
+  void OnHoverActivateTimeout();
+
   void CloseButtonPressed(const ui::Event& event);
 
   void OnTabDataChanged(TabChangeType tab_change_type,
@@ -344,6 +348,9 @@ class Tab : public gfx::AnimationDelegate,
   // enabled and the gesture should not open the tab context menu.
   bool suppress_context_menu_ = false;
 
+  // Timer for activating this tab after the mouse hovers over it.
+  base::OneShotTimer hover_activate_timer_;
+
   std::unique_ptr<TabCloseButtonObserver> tab_close_button_observer_;
 
   std::unique_ptr<tabs::TabDataObserver> tab_data_observer_;
diff --git a/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.cc b/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.cc
index 0640815463641..b60e1eb76bc6c 100644
--- a/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.cc
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.cc
@@ -480,6 +480,14 @@ void VerticalTabView::OnMouseEntered(const ui::MouseEvent& event) {
   CHECK(collection_node_);
   UpdateHoverCard(this, TabSlotController::HoverCardUpdateType::kHover);
 
+  if (base::FeatureList::IsEnabled(features::kHoverActivateTab) &&
+      !IsActive()) {
+    hover_activate_timer_.Start(
+        FROM_HERE, base::Milliseconds(features::kHoverActivateTabDelayMs.Get()),
+        base::BindOnce(&VerticalTabView::OnHoverActivateTimeout,
+                       weak_ptr_factory_.GetWeakPtr()));
+  }
+
   // Hover state is handled by the parent if it is split.
   if (split_) {
     return;
@@ -496,6 +504,8 @@ void VerticalTabView::OnMouseEntered(const ui::MouseEvent& event) {
 void VerticalTabView::OnMouseExited(const ui::MouseEvent& event) {
   CHECK(collection_node_);
 
+  hover_activate_timer_.Stop();
+
   // Hover state is handled by the parent if it is split.
   if (split_) {
     return;
@@ -504,6 +514,20 @@ void VerticalTabView::OnMouseExited(const ui::MouseEvent& event) {
   UpdateHovered(false);
 }
 
+void VerticalTabView::OnHoverActivateTimeout() {
+  if (!collection_node_ || IsActive() || !IsMouseHovered()) {
+    return;
+  }
+
+  if (auto* controller = collection_node_->GetController()) {
+    controller->SelectTab(
+        GetTabInterface(),
+        TabStripUserGestureDetails(
+            TabStripUserGestureDetails::GestureType::kMouse,
+            base::TimeTicks::Now()));
+  }
+}
+
 bool VerticalTabView::OnMouseDragged(const ui::MouseEvent& event) {
   // Protect against key presses when the tab is animating out. Drag events may
   // call this function after the node has been deleted.
diff --git a/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.h b/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.h
index 7f7e9e12c8d5f..faee8d0bd85c2 100644
--- a/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.h
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.h
@@ -11,6 +11,7 @@
 #include "base/callback_list.h"
 #include "base/memory/raw_ptr.h"
 #include "base/scoped_observation.h"
+#include "base/timer/timer.h"
 #include "chrome/browser/ui/tabs/tab_data.h"
 #include "chrome/browser/ui/tabs/tab_style.h"
 #include "chrome/browser/ui/tabs/vertical_tab_strip_state_controller.h"
@@ -191,6 +192,9 @@ class VerticalTabView : public views::View,
   void CloseButtonPressed(const ui::Event& event);
   void RecordMousePressedInTab();
 
+  // Called when the hover-to-activate timer expires.
+  void OnHoverActivateTimeout();
+
   void UpdateHoverCard(HoverCardAnchorTarget* target,
                        int hover_card_update_type);
 
@@ -245,6 +249,10 @@ class VerticalTabView : public views::View,
   // Set between a right-button press and release when right-click-to-close is
   // enabled and the gesture should not open the tab context menu.
   bool suppress_context_menu_ = false;
+
+  // Timer for activating this tab after the mouse hovers over it.
+  base::OneShotTimer hover_activate_timer_;
+
   std::unique_ptr<GlowHoverController> hover_controller_;
   float hover_opacity_min_;
   float hover_opacity_max_;
