diff --git a/chrome/browser/ui/tabs/tab_strip_prefs.cc b/chrome/browser/ui/tabs/tab_strip_prefs.cc
index dc367d3e40bf6..2df0be4499164 100644
--- a/chrome/browser/ui/tabs/tab_strip_prefs.cc
+++ b/chrome/browser/ui/tabs/tab_strip_prefs.cc
@@ -132,6 +132,16 @@ TabSearchPosition GetTabSearchPosition(
              : TabSearchPosition::kLeadingHorizontalTabstrip;
 }
 
+bool IsTabSearchPinningSupported(const BrowserWindowInterface* browser_window) {
+  if (base::FeatureList::IsEnabled(kHorizontalTabStripComboButton)) {
+    return true;
+  }
+
+  const TabSearchPosition position = GetTabSearchPosition(browser_window);
+  return position == TabSearchPosition::kLeadingHorizontalTabstrip ||
+         position == TabSearchPosition::kTrailingHorizontalTabstrip;
+}
+
 void SetTabSearchRightAlignedForTesting(bool is_right_aligned) {
   g_tab_search_trailing_tabstrip_at_startup = is_right_aligned;
 }
diff --git a/chrome/browser/ui/tabs/tab_strip_prefs.h b/chrome/browser/ui/tabs/tab_strip_prefs.h
index b7d6481830eb5..922345fffaa1e 100644
--- a/chrome/browser/ui/tabs/tab_strip_prefs.h
+++ b/chrome/browser/ui/tabs/tab_strip_prefs.h
@@ -38,6 +38,10 @@ void MigrateHoverCardMemoryPref(PrefService* local_prefs);
 TabSearchPosition GetTabSearchPosition(
     const BrowserWindowInterface* browser_window);
 
+// Returns whether the tab search pin preference controls a horizontal tab
+// strip button in the current configuration.
+bool IsTabSearchPinningSupported(const BrowserWindowInterface* browser_window);
+
 void SetTabSearchRightAlignedForTesting(bool is_right_aligned);
 
 }  // namespace tabs
diff --git a/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc b/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc
index 63985d1fed4c9..021ae254abc9e 100644
--- a/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc
+++ b/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc
@@ -46,6 +46,7 @@
 #include "chrome/browser/ui/web_applications/app_browser_controller.h"
 #include "chrome/common/buildflags.h"
 #include "chrome/common/chrome_features.h"
+#include "chrome/common/pref_names.h"
 #include "chrome/grit/generated_resources.h"
 #include "components/commerce/core/commerce_feature_list.h"
 #include "components/vector_icons/vector_icons.h"
@@ -363,6 +364,15 @@ HorizontalTabStripRegionView::HorizontalTabStripRegionView(
     tab_strip_action_container_ =
         AddChildView(std::move(tab_strip_action_container));
   }
+  if (tab_search_button_) {
+    tab_search_pref_registrar_.Init(profile_->GetPrefs());
+    tab_search_pref_registrar_.Add(
+        prefs::kTabSearchPinnedToTabstrip,
+        base::BindRepeating(
+            &HorizontalTabStripRegionView::OnTabSearchPinnedStateChanged,
+            base::Unretained(this)));
+    OnTabSearchPinnedStateChanged();
+  }
   UpdateTabStripMargin();
 }
 HorizontalTabStripRegionView::~HorizontalTabStripRegionView() {
@@ -470,7 +480,8 @@ void HorizontalTabStripRegionView::Layout(PassKey) {
   }
 
   const bool tab_search_button_before_tab_strip =
-      tab_search_button_ && render_tab_search_before_tab_strip_;
+      tab_search_button_ && render_tab_search_before_tab_strip_ &&
+      tab_search_button_->GetVisible();
   if (tab_search_button_before_tab_strip ||
       (unfocus_button_ && unfocus_button_->GetVisible()) || combo_button_) {
     UpdateTabStripMargin();
@@ -769,6 +780,14 @@ void HorizontalTabStripRegionView::UpdateButtonBorders() {
   }
 }
 
+void HorizontalTabStripRegionView::OnTabSearchPinnedStateChanged() {
+  CHECK(tab_search_button_);
+  tab_search_button_->SetVisible(
+      profile_->GetPrefs()->GetBoolean(prefs::kTabSearchPinnedToTabstrip));
+  UpdateTabStripMargin();
+  InvalidateLayout();
+}
+
 void HorizontalTabStripRegionView::UpdateTabStripMargin() {
 #if BUILDFLAG(IS_MAC)
   if (HasLeadingButtons()) {
@@ -803,7 +822,8 @@ void HorizontalTabStripRegionView::UpdateTabStripMargin() {
   std::optional<int> tab_strip_left_margin;
   int current_leading_width = 0;
 
-  if (tab_search_button_ && render_tab_search_before_tab_strip_) {
+  if (tab_search_button_ && render_tab_search_before_tab_strip_ &&
+      tab_search_button_->GetVisible()) {
     // The `tab_search_button_` is being laid out manually.
     CHECK(tab_search_button_->GetProperty(views::kViewIgnoredByLayoutKey));
     current_leading_width +=
diff --git a/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.h b/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.h
index 68d573f1db7d6..1e736e21f96dc 100644
--- a/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.h
+++ b/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.h
@@ -11,6 +11,7 @@
 #include "chrome/browser/ui/views/frame/tab_strip_region_view.h"
 #include "chrome/browser/ui/views/tabs/tab_strip.h"
 #include "chrome/common/buildflags.h"
+#include "components/prefs/pref_change_registrar.h"
 #include "components/tabs/public/tab_interface.h"
 #include "ui/base/metadata/metadata_header_macros.h"
 #include "ui/base/pointer/touch_ui_controller.h"
@@ -138,6 +139,9 @@ class HorizontalTabStripRegionView final : public TabStripRegionView {
   // `render_tab_search_before_tab_strip_` is true.
   void UpdateTabStripMargin();
 
+  // Updates the standalone tab search button when its pin preference changes.
+  void OnTabSearchPinnedStateChanged();
+
   // Gets called on `Layout` and adjusts the x-axis position of the `view` based
   // on `offset`. This should only used for views that show before tab strip.
   void AdjustViewBoundsRect(View* view, int offset);
@@ -155,6 +159,8 @@ class HorizontalTabStripRegionView final : public TabStripRegionView {
   raw_ptr<TabSearchButton> tab_search_button_ = nullptr;
   raw_ptr<TabStripControlButton> unfocus_button_ = nullptr;
 
+  PrefChangeRegistrar tab_search_pref_registrar_;
+
   // On some platforms for Chrome Refresh, the TabSearchButton should be
   // laid out before the TabStrip. Storing this configuration prevents
   // rechecking the child order on every layout.
diff --git a/chrome/browser/ui/views/frame/layout/browser_view_layout_delegate_impl.cc b/chrome/browser/ui/views/frame/layout/browser_view_layout_delegate_impl.cc
index 8ec6ffa9671fc..658b44c125d02 100644
--- a/chrome/browser/ui/views/frame/layout/browser_view_layout_delegate_impl.cc
+++ b/chrome/browser/ui/views/frame/layout/browser_view_layout_delegate_impl.cc
@@ -34,7 +34,7 @@
 BrowserViewLayoutDelegateImpl::BrowserViewLayoutDelegateImpl(
     BrowserView& browser_view)
     : browser_view_(browser_view) {
-  if (base::FeatureList::IsEnabled(tabs::kHorizontalTabStripComboButton)) {
+  if (tabs::IsTabSearchPinningSupported(browser_view_->browser())) {
     PrefService* prefs = browser_view_->GetProfile()->GetPrefs();
     tab_search_pinned_to_tab_strip_ =
         prefs->GetBoolean(prefs::kTabSearchPinnedToTabstrip);
@@ -161,7 +161,7 @@ bool BrowserViewLayoutDelegateImpl::IsActiveTabAtLeadingWindowEdge() const {
     bool has_leading_search_button =
         tabs::GetTabSearchPosition(browser_view_->browser()) ==
         tabs::TabSearchPosition::kLeadingHorizontalTabstrip;
-    if (base::FeatureList::IsEnabled(tabs::kHorizontalTabStripComboButton)) {
+    if (tabs::IsTabSearchPinningSupported(browser_view_->browser())) {
       // Tab search button can be unpinned so including it in the determination
       // of leading edge of horizontal tab strip.
       has_leading_search_button &= tab_search_pinned_to_tab_strip_;
diff --git a/chrome/browser/ui/views/frame/system_menu_model_builder.cc b/chrome/browser/ui/views/frame/system_menu_model_builder.cc
index 0f9cfbf788245..942d46bd1e119 100644
--- a/chrome/browser/ui/views/frame/system_menu_model_builder.cc
+++ b/chrome/browser/ui/views/frame/system_menu_model_builder.cc
@@ -16,6 +16,7 @@
 #include "chrome/browser/ui/browser_element_identifiers.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_features.h"
 #include "chrome/browser/ui/tabs/features.h"
+#include "chrome/browser/ui/tabs/tab_strip_prefs.h"
 #include "chrome/browser/ui/tabs/vertical_tab_strip_state_controller.h"
 #include "chrome/browser/ui/toolbar/app_menu_model.h"
 #include "chrome/browser/ui/ui_features.h"
@@ -158,7 +159,9 @@ void SystemMenuModelBuilder::BuildSystemMenuForBrowserWindow(
                        kNameWindowOldIcon);
 #endif  // BUILDFLAG(IS_MAC)
 
-  if (base::FeatureList::IsEnabled(tabs::kHorizontalTabStripComboButton)) {
+  const bool tab_search_pinning_supported =
+      tabs::IsTabSearchPinningSupported(browser());
+  if (tab_search_pinning_supported) {
     model->AddSeparator(ui::NORMAL_SEPARATOR);
     model->AddItemWithStringId(IDC_TAB_SEARCH_TOGGLE_PIN,
                                IDS_TAB_STRIP_PIN_TAB_SEARCH);
@@ -169,7 +172,7 @@ void SystemMenuModelBuilder::BuildSystemMenuForBrowserWindow(
   // the glic toggle option if glic is enabled when building the menu.
   if (glic::GlicEnabling::IsEnabledForProfile(browser()->profile())) {
 #endif  // BUILDFLAG(IS_WIN)
-    if (!base::FeatureList::IsEnabled(tabs::kHorizontalTabStripComboButton)) {
+    if (!tab_search_pinning_supported) {
       model->AddSeparator(ui::NORMAL_SEPARATOR);
     }
     model->AddItemWithStringId(IDC_GLIC_TOGGLE_PIN, IDS_GLIC_PIN);
diff --git a/chrome/browser/ui/views/frame/system_menu_model_delegate.cc b/chrome/browser/ui/views/frame/system_menu_model_delegate.cc
index 398fad74e65b1..5556ced407871 100644
--- a/chrome/browser/ui/views/frame/system_menu_model_delegate.cc
+++ b/chrome/browser/ui/views/frame/system_menu_model_delegate.cc
@@ -16,6 +16,7 @@
 #include "chrome/browser/ui/browser_window.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_features.h"
 #include "chrome/browser/ui/tabs/features.h"
+#include "chrome/browser/ui/tabs/tab_strip_prefs.h"
 #include "chrome/browser/ui/tabs/vertical_tab_strip_metrics.h"
 #include "chrome/browser/ui/tabs/vertical_tab_strip_state_controller.h"
 #include "chrome/browser/ui/ui_features.h"
@@ -67,7 +68,7 @@ bool SystemMenuModelDelegate::IsCommandIdEnabled(int command_id) const {
   }
 #endif
   if (command_id == IDC_TAB_SEARCH_TOGGLE_PIN) {
-    return base::FeatureList::IsEnabled(tabs::kHorizontalTabStripComboButton);
+    return tabs::IsTabSearchPinningSupported(browser_);
   }
   // Disable the glic toggle pin if it is showing and glic is not enabled.
   if (command_id == IDC_GLIC_TOGGLE_PIN) {
@@ -113,7 +114,7 @@ bool SystemMenuModelDelegate::IsCommandIdVisible(int command_id) const {
   }
 #endif
   if (command_id == IDC_TAB_SEARCH_TOGGLE_PIN) {
-    return base::FeatureList::IsEnabled(tabs::kHorizontalTabStripComboButton);
+    return tabs::IsTabSearchPinningSupported(browser_);
   }
   if (command_id == IDC_GLIC_TOGGLE_PIN) {
     return glic::GlicEnabling::IsEnabledForProfile(browser_->profile());
@@ -229,7 +230,7 @@ void SystemMenuModelDelegate::ExecuteCommand(int command_id, int event_flags) {
       break;
     }
     case IDC_TAB_SEARCH_TOGGLE_PIN: {
-      if (base::FeatureList::IsEnabled(tabs::kHorizontalTabStripComboButton)) {
+      if (tabs::IsTabSearchPinningSupported(browser_)) {
         PrefService* prefs = browser_->profile()->GetPrefs();
         const bool is_pinned =
             prefs->GetBoolean(prefs::kTabSearchPinnedToTabstrip);
